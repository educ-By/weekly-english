# Weekly English — dependencies for both CLI and FastAPI server

requests>=2.31
feedparser>=6.0.10
beautifulsoup4>=4.12
lxml>=4.9
jinja2>=3.1

# Server
fastapi>=0.110
uvicorn[standard]>=0.27
apscheduler>=3.10
python-dotenv>=1.0
openai>=1.30            # DeepSeek API client (OpenAI-compatible)

# Database & auth
sqlalchemy>=2.0
psycopg[binary]>=3.1    # Postgres driver (Neon compatible)
bcrypt>=4.1             # 直接用 bcrypt,不经过 passlib(兼容性好)
python-jose[cryptography]>=3.3
email-validator>=2.0

# PDF (optional)
weasyprint>=60.0